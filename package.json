{
  "name": "my-life-admin-assistant",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "install": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "backend": "cd backend && .\\venv\\Scripts\\activate && uvicorn app.main:app --reload"
  }
}
